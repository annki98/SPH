
cmake_policy(SET CMP0104 OLD)

# add_executable(CudaGrid particlegrid.cu)

# target_compile_options(CudaGrid PRIVATE $<$<COMPILE_LANGUAGE:CUDA>:${CUDA_OPTIONS}>)
# set_target_properties(CudaGrid PROPERTIES CUDA_SEPARABLE_COMPILATION ON)
